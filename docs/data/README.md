# Data Directory

This folder stores datasets used in the phishing email and malicious URL classification project.

## Structure

### raw/
Contains original datasets exactly as downloaded.

### processed/
Contains cleaned and transformed datasets created during preprocessing.

## Notes
- Raw datasets should never be modified directly.
- Large datasets are excluded from Git using .gitignore.
- If datasets become too large, Git LFS or cloud storage can be used.